# Contributing

Keep new relationship content original. Do not copy proprietary questions, art, branding or layouts from commercial reference apps.

Before committing:

```bash
gradle :app:testDebugUnitTest
gradle :app:assembleDebug
```

Do not commit API keys, service-account files, signing keys or local Android SDK paths.
